---
import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";
import LessonSearch from "../components/LessonSearch.astro";

const lessons = await getCollection("lessons");
lessons.sort((a, b) => a.data.title.localeCompare(b.data.title));
---

<Layout title="Hebrew Tips & Notes">
  <main>
    <h1>Hebrew Tips & Notes</h1>
    <p class="subtitle">
      Complete Tips and Notes from the Duolingo Hebrew course.
      These notes were removed from Duolingo and are preserved here as a reference.
    </p>

    <section>
      <h2>All Lessons</h2>
      <LessonSearch placeholder="Search lessons..." noResultsText="No lessons match your search." />
      <ul class="lesson-list" id="lesson-list">
        {
          lessons.map((lesson) => (
            <li data-title={lesson.data.title.toLowerCase()}>
              <a href={`/lessons/${lesson.id}`}>{lesson.data.title}</a>
            </li>
          ))
        }
      </ul>
    </section>
  </main>
</Layout>

<style>
  main {
    max-width: 800px;
    margin: 0 auto;
    padding: var(--space-16) var(--space-14);
  }

  h1 {
    font-size: var(--font-size-3xl);
    margin-bottom: var(--space-4);
  }

  .subtitle {
    color: var(--color-text-muted);
    margin-bottom: var(--space-14);
    line-height: var(--line-height-base);
  }

  .lesson-list {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: var(--space-4);
    list-style: none;
    padding: 0;
  }

  .lesson-list a {
    display: block;
    padding: var(--space-7) var(--space-9);
    background: var(--color-bg-subtle);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    color: var(--color-accent);
    font-size: var(--font-size-base);
    transition:
      background var(--transition-fast),
      border-color var(--transition-fast);

    &:hover {
      background: var(--color-bg);
      border-color: var(--color-accent);
    }
  }

  @media (max-width: 768px) {
    main {
      padding: var(--space-12) var(--space-11);
    }
  }
</style>
